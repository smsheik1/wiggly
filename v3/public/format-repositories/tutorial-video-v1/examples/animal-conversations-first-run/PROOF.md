# Animal Conversations first-run proof

This file is an evidence receipt for the package-produced example. The source
master is the recovered, QA-passed Animal Conversations tutorial workflow; the
output was created by `node runner.mjs render` from `input.json`.

The tutorial shows the finished result first, then the real Wiggly handoff to a
computer-capable coding agent, local source preparation, explicit approval,
local rendering, inspection, and delivery. Narration and captions are supplied
by the reviewed master. No paid model or media call was made.
